export const metadata = {
	title: "Sanity Studio - Personal Finance Tracker",
	description: "Manage content for Personal Finance Tracker",
};

export default function StudioLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
